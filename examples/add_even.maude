fmod FOO is
 pr NAT .

 vars N N' : Nat .

 op add : Nat Nat -> Nat .
 eq add(N, N') = N + N' .

 op even : Nat -> Bool .
 eq even(0) = true .
 eq even(1) = false .
 eq even(s(s(N))) = even(N) .
endfm